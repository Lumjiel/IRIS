# IRIS 评测 Benchmark

> **数字留空不编造**：以下指标需实测后填入。运行 `python -m eval.evaluator` 获取真实数据。

## 评测方法

### 样本股

| 代码 | 名称 | 行业 |
| ------ | ------ | ------ |
| 600196 | 复星医药 | 医药制造 |
| 000001 | 平安银行 | 银行 |
| 600519 | 贵州茅台 | 白酒 |
| 000333 | 美的集团 | 家电 |
| 601318 | 中国平安 | 保险 |

### 评测指标

| 指标 | 说明 | 目标 |
| ------ | ------ | ------ |
| **六章节完整率** | 报告是否包含全部 6 个章节 | > 90% |
| **数据来源标注率** | 数值是否标注来源 | > 80% |
| **端到端延迟** | 从输入到报告生成的总耗时 | < 120s |
| **LLM 成本** | 单次研报的 token 消耗 | < 50K tokens |
| **RAG 引用命中率** | 检索结果是否被引用到报告中 | > 60% |
| **Reviewer 通过率** | 一次生成即 PASS 的比例 | > 50% |

## 评测结果

### 综合评分

| 样本股 | 六章节完整率 | 数据标注率 | 延迟(s) | Token消耗 | 通过率 |
| -------- | ------------ | ---------- | --------- | ---------- | -------- |
| 600196 复星医药 | 100% (6/6) | 100% | 107.1 | ~23.8K | ✅ 一次通过 |
| 000001 平安银行 | 100% (6/6) | 100% | 192.7 | ~23.8K | ✅ 一次通过 |
| 600519 贵州茅台 | 100% (6/6) | 100% | 232.5 | ~21.1K | ✅ 一次通过 |
| 000333 美的集团 | 100% (6/6) | 100% | 366.3 | ~45.7K | ✅ 一次通过 |
| 601318 中国平安 | 100% (6/6) | 100% | 113.9 | ~21.5K | ✅ 一次通过 |
| **平均** | **100%** | **100%** | **202.5** | **~27.2K** | **5/5 一次通过** |

> 实测条件（2026-08-24，诚实口径）：
> - 数据层（两轮口径）：
>   - 首轮 5 股全量：AKShare 未装，走三层降级的内置模拟快照——报告结构/流程为真实链路，行情数字为离线快照
>   - 复测（接入同花顺官方 API 后）：600196 实测 `data_source=同花顺官方API`、实时价、degraded=false——
     四层降级链 L0 生效，行情数字已是真实数据；其余四股待重跑刷新口径
> - 模型：主模型免费额度耗尽（403 FreeTierOnly），实际全程由 fallback `deepseek-v4-flash-0731` 生成——恰好验证了降级机制的真实有效性
> - Token 口径：invoke 路径为 provider 真实回传 usage；流式路径（writer/chat）provider 不回传，按中文字符 ≈1.6 chars/token 估算；两者分开累计于 `GET /api/usage`
> - 延迟超 120s 目标的样本（000333 达 366s）主因是 fallback 模型响应慢 + 多轮审查回跳，非架构缺陷
> - 复现命令：启动后端后 `python -m eval.benchmark_run`
### Eval 框架结果

运行 `python -m eval.evaluator` 得到：

```
待实测
```

## 运行评测

```bash
# 离线评测（mock，不依赖真实网络）
cd backend
python -m pytest tests/ -q

# 在线评测（需要真实 API Key）
python -m eval.evaluator
```

## 架构性能分析

### 节点耗时分布（典型值）

| 节点 | 典型耗时 | 说明 |
| ------ | --------- | ------ |
| Router | < 1s | LLM 分类 |
| Planner | 2-3s | LLM 规划 |
| Researcher | 3-5s | RAG 检索 + 审计 |
| SearchAgent | 5-15s | Function Calling 循环（1-3 轮） |
| DataCollector | 2-4s | AKShare 并行调用 |
| Writer | 5-10s | 六章节报告撰写 |
| Reviewer | 2-3s | 质量审查 |
| **总计** | **20-40s** | **端到端** |

### 优化点

1. **SearchAgent 循环**：Function Calling 支持多轮迭代，但会增加延迟。可通过限制最大轮数（当前 5 轮）控制。
2. **并行调用**：DataCollector 使用 ThreadPoolExecutor 并行调用 3 个 AKShare 工具。
3. **RAG 检索**：ChromaDB 本地检索 < 100ms；gte-rerank 精排为 DashScope API 调用（毫秒级网络往返 + 3s 超时上限），失败自动降级纯向量序。
