# IRIS 评测 Benchmark

> **数字留空不编造**：以下指标需实测后填入。运行 `python -m eval.evaluator` 获取真实数据。

## 评测方法

### 样本股

| 代码 | 名称 | 行业 |
| ------ | ------ | ------ |
| 600196 | 复星医药 | 医药制造 |
| 000001 | 平安银行 | 银行 |
| 600519 | 贵州茅台 | 白酒 |
| 000333 | 美的集团 | 家电 |
| 601318 | 中国平安 | 保险 |

### 评测指标

| 指标 | 说明 | 目标 |
| ------ | ------ | ------ |
| **六章节完整率** | 报告是否包含全部 6 个章节 | > 90% |
| **数据来源标注率** | 数值是否标注来源 | > 80% |
| **端到端延迟** | 从输入到报告生成的总耗时 | < 120s |
| **LLM 成本** | 单次研报的 token 消耗 | < 50K tokens |
| **RAG 引用命中率** | 检索结果是否被引用到报告中 | > 60% |
| **Reviewer 通过率** | 一次生成即 PASS 的比例 | > 50% |

## 评测结果

### 综合评分

| 样本股 | 六章节完整率 | 数据标注率 | 延迟(s) | Token消耗 | 通过率 |
| -------- | ------------ | ---------- | --------- | ---------- | -------- |
| 600196 复星医药 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |
| 000001 平安银行 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |
| 600519 贵州茅台 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |
| 000333 美的集团 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |
| 601318 中国平安 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |
| **平均** | **待实测** | **待实测** | **待实测** | **待实测** | **待实测** |

### Eval 框架结果

运行 `python -m eval.evaluator` 得到：

```
待实测
```

## 运行评测

```bash
# 离线评测（mock，不依赖真实网络）
cd backend
python -m pytest tests/ -q

# 在线评测（需要真实 API Key）
python -m eval.evaluator
```

## 架构性能分析

### 节点耗时分布（典型值）

| 节点 | 典型耗时 | 说明 |
| ------ | --------- | ------ |
| Router | < 1s | LLM 分类 |
| Planner | 2-3s | LLM 规划 |
| Researcher | 3-5s | RAG 检索 + 审计 |
| SearchAgent | 5-15s | Function Calling 循环（1-3 轮） |
| DataCollector | 2-4s | AKShare 并行调用 |
| Writer | 5-10s | 六章节报告撰写 |
| Reviewer | 2-3s | 质量审查 |
| **总计** | **20-40s** | **端到端** |

### 优化点

1. **SearchAgent 循环**：Function Calling 支持多轮迭代，但会增加延迟。可通过限制最大轮数（当前 5 轮）控制。
2. **并行调用**：DataCollector 使用 ThreadPoolExecutor 并行调用 3 个 AKShare 工具。
3. **RAG 检索**：ChromaDB 本地检索 < 100ms，CrossEncoder rerank 额外 +200ms。
